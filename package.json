{
	"name": "exam-assist",
	"version": "2.0.0",
	"description": "AI cheating extension to pass tests ;) ",
	"type": "module",
	"scripts": {
		"build": "node build.js",
		"build:obfuscate": "node build.js --obfuscate",
		"build:firefox": "node build.js --firefox",
		"build:firefox:obfuscate": "node build.js --firefox --obfuscate",
		"showcase": "python3 -m http.server 3000 -d showcase",
		"deploy": "git add . && git commit -m 'Deploy showcase' && git push origin main"
	},
	"dependencies": {
		"i18next": "^25.6.0"
	},
	"devDependencies": {
		"esbuild": "^0.25.10",
		"javascript-obfuscator": "^4.1.1"
	}
}
