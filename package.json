{
	"name": "Screenshot Selector",
	"version": "1.1.0",
	"description": "Select an area on the page and copy it as an image to the clipboard",
	"type": "module",
	"scripts": {
		"build": "node build.js",
		"build:obfuscate": "node build.js --obfuscate",
		"build:firefox": "node build.js --firefox",
		"build:firefox:obfuscate": "node build.js --firefox --obfuscate"
	},
	"dependencies": {
		"i18next": "^25.6.0"
	},
	"devDependencies": {
		"esbuild": "^0.25.10",
		"javascript-obfuscator": "^4.1.1"
	}
}
